language: go

go:
  - "1.11.x" # debian buster
  - "1.13.x" # ubuntu focal
  - "1.x"    # latest release

addons:
  apt:
    packages:
      - golint

script:
  - go vet ./...
  - golint ./...
  - go build -v ./...
  - go test -v -cover ./...
